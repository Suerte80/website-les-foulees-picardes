{{ form_start(form, {
    attr: {
        id: 'change-password-form',
        'data-ajax-form-target': 'form'
    }
}) }}
  {{ form_errors(form) }}

  {{ form_row(form.oldPassword) }}

  {# RepeatedType : afficher les 2 champs pour bien voir les erreurs #}
  {{ form_row(form.newPassword.first) }}
  {{ form_row(form.newPassword.second) }}

  {{ form_row(form.submit) }}
{{ form_end(form) }}
